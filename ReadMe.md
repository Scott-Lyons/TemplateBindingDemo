### Comparison of NodeJs template libraries

#### To Run
Navigate to `\src` folder and enter typescript compiler command `tsc` in console.

A `build` folder will be generated in the `src` folder. Run `index.js` as a node application.